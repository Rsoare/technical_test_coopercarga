import { styled } from "styled-components";

export const UlStyled = styled.div`
   display: flex;
   flex-direction: row;
   gap: 4rem;
   justify-content: center;
   width: 100%;

   li {
      cursor: pointer;
   }
`;
